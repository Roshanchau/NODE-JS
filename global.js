//Global oject in node js

// console.log(global);

// setTimeout(() => {
//   console.log("in the timeout");
//   clearInterval(int);
// }, 3000);

// const int = setInterval(() => {
//   console.log("in the interval");
// }, 1000);

// console.log(__dirname);
// console.log(__filename);

//the global object of browser and the node is different we cannot acess the document object in the node but we can do that in the window global object.

// console.log(document.querySelector); ==> throws an error since it is not defined in the global object of the node.
